# Bonin QR Menü

Mobil öncelikli dijital menü. Canlı adres: ana sayfa (`/`). Supabase tanımlı değilse yerel seed verisiyle açılır.

```bash
npm install
npm run dev
```

Supabase için `.env.example` dosyasını `.env.local` olarak kopyalayıp `supabase/schema.sql` dosyasını projeye uygulayın.
